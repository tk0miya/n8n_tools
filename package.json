{
  "name": "ghscan",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "ghscan": "./dist/ghscan.mjs"
  },
  "scripts": {
    "build": "esbuild src/cli.ts --bundle --platform=node --format=esm --outfile=dist/ghscan.mjs --banner:js='#!/usr/bin/env node' --alias:@=./src",
    "test": "vitest run --passWithNoTests",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "ci": "npm run lint && npm test && npm run build"
  },
  "dependencies": {
    "@octokit/rest": "^22.0.1",
    "dotenv": "^17.3.1",
    "js-yaml": "^4.1.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.10",
    "@types/dedent": "^0.7.2",
    "@types/js-yaml": "^4.0.9",
    "dedent": "^1.7.2",
    "esbuild": "^0.27.4",
    "typescript": "^6.0.2",
    "vitest": "^4.1.2"
  }
}
